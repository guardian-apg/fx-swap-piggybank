{
  "name": "fx-swap-piggybank",
  "version": "1.0.0",
  "description": "FX Swap Piggy Bank App",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step required for static SPA'",
    "cap:sync": "cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
